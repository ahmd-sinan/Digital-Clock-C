# Digital Clock using C ⌚

A simple console-based Digital Clock application written in C.  
The program takes a starting time from the user and updates the clock every second.

## 🔧 Features
- User-defined starting time (Hours, Minutes, Seconds)
- Real-time clock update (1 second delay)
- Proper time validation
- Auto reset of seconds, minutes, and hours
- Clean digital time format (HH:MM:SS)

## 🧠 Concepts Used
- Loops (`while`)
- Conditional statements
- Time logic
- `Sleep()` function
- Console screen clearing
- Input validation

## 🛠️ Requirements
- C Compiler (GCC / MinGW)
- Windows OS (uses `windows.h`)
